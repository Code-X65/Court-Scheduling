# Frontend Gap Analysis — Court Scheduling System

Generated: 2026-05-16  
Scope: React 18 / Vite frontend at `Frontend/`

---

## 1. What Is Completed in the Frontend

### Routing & Layout
| Item | Detail |
|------|--------|
| React Router v6 | All 12 routes declared in `App.jsx` with lazy-loaded page chunks |
| Route guards | `RequireAuth` component; unauthenticated users redirected to `/login` |
| Layout shell | Flex row: fixed-width `Sidebar` (260 px) + flexible `main-content` area |
| Sticky Navbar | Backdrop `position: sticky`, theme toggle, user dropdown, sign-out |
| 404 / catch-all | `Navigate to="/dashboard"` for any unmatched path |

### Authentication & Authorization
| Item | Detail |
|------|--------|
| Login page | `Login.jsx` — username/password form, role inference from username |
| Auth context | `AuthContext` — `login()`, `logout()`, `isAuthenticated` via `sessionStorage` |
| Role-based sidebar | `Sidebar.jsx` — admin sees all links; clerk sees 5 links; judge sees 3 |
| Session expiry redirect | API 401 interceptor → clears `sessionStorage` → `window.location.href = '/login?expired=1'` |

### Global UX Features
| Item | Detail |
|------|--------|
| Dark / light theme | Toggle saves to `localStorage`; `data-theme` drives CSS custom properties |
| Toast system | `Toast.jsx` — controller + `toast()` helper; 4 s auto-dismiss (success/error/warning/info) |
| Keyboard shortcuts | `useUX.js` — Ctrl+Z undo, Ctrl+Y redo, `/` focus-search (Cases page) |
| Undo / Redo stack | `useHistory()` hook — action log with `pushAction`, `undo`, `redo` |
| Dashboard widget customization | Checkboxes + drag-order persisted in `localStorage` via `PreferencesContext` |
| Drag-and-drop hearing reschedule | Calendar page — HTML5 drag API + PATCH `/hearings/:id` |
| Optimistic mutations | `useQuery` + `useMutation` hooks with rollback on error |
| Skeleton loaders | `.skeleton` CSS class reused across all table/list loading states |
| Print stylesheet | `@media print` hides navbars, strips box-shadows |

### Pages (all 12 exist)
| Route | File | Status |
|-------|------|--------|
| `/login` | `Login.jsx` | Complete |
| `/dashboard` | `Dashboard.jsx` | Complete |
| `/cases` | `Cases.jsx` | Complete |
| `/judges` | `Judges.jsx` | Complete |
| `/courtrooms` | `Courtrooms.jsx` | Complete |
| `/schedule` | `Schedule.jsx` | Complete |
| `/calendar` | `Calendar.jsx` | Complete |
| `/reports` | `Reports.jsx` | Complete |
| `/users` | `Users.jsx` | Complete |
| `/settings` | `Settings.jsx` | Complete |
| `/ai-insights` | `AIInsights.jsx` | Complete |
| `/profile` | `Profile.jsx` | Complete |

### Components (8 in `src/components/`)
| Component | File | Purpose |
|-----------|------|---------|
| `Navbar` | `Navbar.jsx` | Sticky top bar |
| `Sidebar` | `Sidebar.jsx` | Role-filtered nav rail |
| `Modal` | `Modal.jsx` | Reusable overlay dialog |
| `ConfirmDialog` | `ConfirmDialog.jsx` | Confirm / cancel modal |
| `Toast` / `toast()` | `Toast.jsx` | Notification system |
| `ScheduleGrid` | `ScheduleGrid.jsx` | Weekly hearing timetable with conflict highlighting |
| `ConflictMatrix` | `ConflictMatrix.jsx` | Day × hour conflict heat-map |
| `StatusBadge` / `PriorityDot` | `StatusBadge.jsx` | Coloured inline badges |

### Hooks (`src/hooks/`)
| Hook | File | Purpose |
|------|------|---------|
| `useQuery` | `useQuery.js` | GET with in-memory cache + `invalidate()` |
| `useMutation` | `useQuery.js` | POST/PUT/DELETE with optimistic callbacks |
| `useKeyboardShortcuts` | `useUX.js` | Registers key listeners |
| `useHistory` | `useUX.js` | Undo/redo action stack |

### Contexts
| Context | File | State |
|---------|------|-------|
| `AuthContext` | `AuthContext.jsx` | `user`, `login`, `logout`, `isAuthenticated`, `RoleGuard` |
| `PreferencesContext` | `PreferencesContext.jsx` | `preferences`, `updatePreference` (persisted to `localStorage`) |

### Styling (`src/styles/global.css`)
| Area | Coverage |
|------|----------|
| Design tokens | 30+ CSS custom properties (light + dark themes) |
| Typography | Libre Baskerville (headings) + DM Sans (body) |
| Layout | `.layout-shell`, `.sidebar`, `.main-content`, `.page-content`, `.page-header` |
| Controls | `.btn` primary/secondary/danger/amber/sm/icon variants |
| Forms | `.form-group`, `.form-input`, `.form-select`, `.form-grid`, `.form-error`, `.form-help` |
| Checkboxes & radios | `.checkbox-pill`, `.checkbox-group`, `.radio-pill`, `.radio-group` |
| Tables | `.data-table`, `.table-wrapper`, `.table-empty`, `.table-actions` |
| Cards | `.card` with shadow + border-radius |
| Skeleton | à la Twitter shimmer animation |
| Info boxes | blue / amber / red / green variants |
| Spinner | CSS-only rotating circle |
| Print | `@media print` hides `.no-print` elements |
| Scrollbar | WebKit thin custom scrollbar |

### API Layer (`src/api/client.js`)
| Feature | Detail |
|---------|--------|
| Axios instance | Base URL from `VITE_API_BASE_URL` or `http://localhost:8000` |
| Request interceptor | Injects `Authorization: Basic <creds>` from `sessionStorage` on every request |
| Response interceptor | On 401 → clears session + redirects to `/login?expired=1` |

### Tests (`src/tests/`)
| File | What's covered |
|------|----------------|
| `useQuery.test.js` | Happy-path fetch + error handling (Vitest + @testing-library) |
| `Badge.test.js` | Renders correctly for `pending`/`scheduled`, handles unknown value |

---

## 2. What Is NOT Completed in the Frontend

### Pages with Mock / Hard-coded Data (no real backend call)
| Page / Area | Issue |
|-------------|-------|
| `Users.jsx` (Users) | Uses `setUsers([... hardcoded array ...])` — never calls the API |
| `Reports.jsx` | `fetchAnalytics()` returns a local object, not a real API response |
| `AIInsights.jsx` | Falls back to inline mock data if `/ai/stats` throws |
| `Settings.jsx` — Holidays table | `holidays` state seeded with hardcoded objects |
| `Settings.jsx` — Audit trail | `auditLogs` state seeded with hardcoded objects |
| `Calendar.jsx` — `DailyView` | `today = '2024-05-20'` hardcoded mock date |
| `Calendar.jsx` — `MonthlyView` | `daysInMonth = 30`, start offset `Array(5).fill(0)` hardcoded |

### Auth Gaps
| Gap | Detail |
|-----|--------|
| No real password validation | Login just checks `username + password are non-empty` |
| `sessionStorage`-only auth | Session lost on tab/browser close; no `localStorage` / cookie / token refresh fallback |
| No SessionStorage expiry | No TTL check on the stored auth object |
| Login `catch` path | `catch` block references `setLoading(false)` but variable name is `loading` (shadowed/correct but flow skipped on success) |
| No "Remember Me" / persistent login | |
| No forgot-password / reset-password flow | |
| No "API-only" auth mode (JWT, Bearer token, OAuth, etc.) | Only Basic Auth via interceptor |

### `Days.jsx` Critical Defects
| Gap | File:Line | Detail |
|-----|-----------|--------|
| Wrong import path | `App.jsx:14` | `import Judges from './pages/Judges.jsx'` — tests `None such file or directory` when typing this task in |
| `console.log` before components declared | `App.jsx:12-21` | Lines 12–21 are the `const PageLoader = …` router setup, but the lazy `import()` paths reference files that don't actually work for the static analyser, which is odd since they do load fine at runtime. |

### Cases Page Gaps
| Gap | Detail |
|-----|--------|
| No form field: `status` | Status is decided by the scheduler; cases are created as `pending` with no status picker |
| No bulk action buttons | Multi-select bar (`selectedIds`) has "Deselect All" but no bulk-edit or bulk-delete action |
| Case number custom-format | No format helper/inline hint beyond the placeholder |

### Judges Page Gaps
| Gap | Detail |
|-----|--------|
| No `email`, `phone`, `rank`, `gender`, `division` fields | Backend model has these; frontend form exposes only name + specializations + available days + max/day |
| Modal reuse hack in List.js | Modal state is lifted and shared with ConfirmDialog, coupling the two components unnecessarily |

### Courtrooms Page Gaps
| Gap | Detail |
|-----|--------|
| No `location`, `equipment` fields | Backend model has `location` and `equipment`; frontend form exposes only name + capacity |
| No batch activate/deactivate | |

### Calendar / Schedule Gaps
| Gap | Detail |
|-----|--------|
| `DailyView` hardcoded date | Always shows `2024-05-20` |
| `MonthlyView` hardcoded | Mock grid with no real date math |
| No "add hearing" creation from calendar | Drag-to-reschedule exists, but adding new hearings from the calendar is absent |
| No timezone support | All date math is naive local time |
| Calendar uses `id` to match courtrooms in select | No `.name` display enhancement |

### Reports Gaps
| Gap | Detail |
|-----|--------|
| No real export | `handleExport()` calls `toast()` only — no file download, no CSV/PDF/iCal generation |
| No real send-email | `handleEmailStakeholders()` just calls `toast()` |
| "Recently Exported" table is hardcoded | |

### Settings Gaps
| Gap | Detail |
|-----|--------|
| Holiday "Add" button has no handler | `+ Add Holiday` button is wired to `setShowAdd(false)` but no modal exists |
| General Settings form not connected to API | `handleSave` just calls `toast()` |
| Notification APIs (Twilio/SendGrid) shown as disabled | Keys are hardcoded as `ACxxxxxxxxxxxxxxxxxxxxxxxx` without a real key-mgmt flow |
| System restore is disabled with no explanation | |

### Users Page Gaps
| Gap | Detail |
|-----|--------|
| No role-edit flow after creation | `handleAdd()` creates a user but has no re-open / edit mode |
| No password-reset API integration | Shows `toast()` mock only |
| No invite / activation-email flow | |

### AI Insights Gaps
| Gap | Detail |
|-----|--------|
| `Retrain Model` just calls `toast()` | Should hit `/ai/train` endpoint and poll for status |
| Mock fallback data never changes | Even if backend is available, the catch block is broad enough to mask real failures |

### Missing Global Infrastructure
| Gap | Detail |
|-----|--------|
| No `ErrorBoundary` | An unhandled component render error drops the whole app with no recovery UI |
| No `.env.example` | Developers have no template for `VITE_API_BASE_URL` or feature toggles |
| No `base.css` / `index.css` | `global.css` is imported in `main.jsx`, but there is no `index.css` — lazy chunks may miss it |
| No lint / typecheck scripts in `package.json` | Only `dev`, `build`, `preview` are defined |
| No `vite-env.d.ts` | `import.meta.env` is untyped |
| No API versioning | All calls go to `/al` roots; no `/api/v1/…` path structure |
| No request/response logging in dev | No axios interceptor for console-debugging in non-production |
| No `--fix` lint commit hook | |

### Missing UX / Accessibility
| Gap | Detail |
|-----|--------|
| No skip-link for keyboard users | |
| Navbar "breadcrumb / search" placeholder is empty | `Navbar.jsx:31` — intentionally blank |
| No ARIA landmarks beyond sidebar `role="navigation"` | |
| No focus trapping in `Modal.jsx` | Focus can escape to the page behind the modal |
| No `aria-expanded` / `aria-controls` on collapse-toggles (sidebar) | Sidebar has no collapse / expand toggles |
| No `sr-only` labels for icon-only buttons | |
| No responsive breakpoints | No `@media` rules for tablets / mobile (aside from print) |

### Missing Test Infrastructure
| Gap | Detail |
|-----|--------|
| No test runner config | No `vitest.config.*`, no `setupTests.js` |
| No `describe` for pages | Only hooks and Badge tested; 0 page-component tests |
| No `describe` for contexts | `AuthContext`, `PreferencesContext` untested |
| No `describe` for API client | Client interceptors untested |
| `Badge.test.js` references non-existent `react` element confusion | Vitest expects `screen.getByText` to return a DOM element, but the test assert `toBeDefined()` works — fine but brittle |
| No coverage report | No `vitest --coverage` config |

### Duplicate / Confusing Routing
| Gap | Detail |
|-----|--------|
| Backend DB uses snake_case (`judge_id`, `courtroom_id`) | Frontend displays camelCase aliases (`assignedJudgeName`, etc.) — no mapping layer documented |
| Cases `judge_id` field set in form | `form.assigned_judge_id` is the field key in `Cases.jsx:79`, matching the backend, but no type safety guards |
| Upgrading from 4 to 5 judges in a courtroom should trigger a new conflict check | Conflict detection in `ScheduleGrid.jsx` is purely based on pre-computed `conflicts` Set — no live recalculation after grid updates |

### Unhandled Loading States
| Gap | Detail |
|-----|--------|
| `Schedule.jsx` `result.publish` call | `isPublishing` spinner but no button disabled-state text |
| `Schedule.jsx` `case_ids: selectedIds` can be empty | Button is `disabled` but no error boundary around case selection |
| `Dashboard.jsx` skeleton height | `1rem — height` fixed; does not scale for different theme font sizes |
| `Reports.jsx` `fetchAnalytics` loading | Uses `setLoading(true)` but skeleton is rendered only if `!data` — race condition if `setData` fires before `finally` sets loading false |

---

## 4. What Needs to be Added to the Frontend

### Must-Have (blockers for production)
| Priority | Item | Notes |
|----------|------|-------|
| 🔴 High | **ErrorBoundary** | Wrap `<App />` — catch render errors, show retry UI |
| 🔴 High | **real auth flow** | Replace mock login with JWT / OAuth2 / OIDC; add refresh-token interceptor |
| 🔴 High | **API wiring for Users page** | Replace hardcoded mock array with `GET /users`, `POST /users`, etc. |
| 🔴 High | **reconnect globals.css** | Confirm `global.css` import reaches all lazy bundles or add a shared reset in `main.jsx` |
| 🔴 High | **Env template** | Add `Frontend/.env.example` |
| 🔴 High | **Holiday modal** | Settings → Holidays: the `+ Add Holiday` button must open a form that calls `POST /holidays` |
| 🔴 High | **Report export** | `handleExport()` must generate and download actual CSV / PDF / iCal files |

### Should-Have (near-term)
| Priority | Item | Notes |
|----------|------|-------|
| 🟡 Medium | **Undo on Case deletes** | Already uses `pushAction` — but only pushes the second action pair; first action pair is undefined. Fix: push the full action pair and show toast only after `execute`. |
| 🟡 Medium | **Hearing creation from calendar** | "New hearing" button on Calendar → lightning-dialog with case, courtroom, start-time fields |
| 🟡 Medium | **Forget/reset password** | `/forgot-password` page + reset-link email notification |
| 🟡 Medium | **Judge form extended fields** | Add `email`, `phone`, `rank`, `gender`, `division` fields matching backend model |
| 🟡 Medium | **Courtroom extended fields** | Add `location`, `equipment` fields |
| 🟡 Medium | **Cases form — status field** | Allow status override when scheduling (not forced to `pending`) |
| 🟡 Medium | **Bulk case actions** | Bind "Bulk Edit" + "Bulk Delete" buttons in the multi-select bar |
| 🟡 Medium | **Date/time picker component** | Replace raw `<input type="date">` with a branded date-picker (e.g., `react-datepicker` or native `<input type="datetime-local">`) |
| 🟡 Medium | **Pagination for long tables** | Replace `Load More` with proper page/size controls |
| 🟡 Medium | **Focus trap in Modal** | Prevent keyboard users from tabbing out of the dialog while open |
| 🟡 Medium | **Skip link at body top** | `<a href="#main-content" class="sr-only">Skip to main content</a>` |
| 🟡 Medium | **Sentry / error logging** | Wrap client-side errors and log to an observability platform |
| 🟡 Medium | **Breadcrumbs component** | Auto-generate from route path; place in `Navbar.jsx` where placeholder lives |
| 🟡 Medium | **Real-time calendar updates** | WebSocket / SSE for live hearing changes (especially when multiple users are editing simultaneously) |

### Nice-to-Have (future)
| Priority | Item | Notes |
|----------|------|-------|
| 🟢 Low | **PWA manifest** | Installable web-app shell |
| 🟢 Low | **Voice-over navigation** | ARIA live regions for new toasts |
| 🟢 Low | **Keyboard-only calendar reschedule** | Arrow keys select cell, Enter opens date/time dialog |
| 🟢 Low | **Dark mode HUD for calendar** | Annotate judge workload directly in the grid |
| 🟢 Low | **Fuzzy / fuzzy-search** | Replace simple `includes` with Fuse.js or meilisearch |
| 🟢 Low | **iCal calendar subscription feed** | `/api/schedule.ics` endpoint + `Subscribe` button |

---

## Summary of Deficit

| Category | Count |
|----------|-------|
| Completed pages | 12 / 12 |
| Completed components | 8 / 8 |
| Completed hooks | 4 / 4 |
| Completed contexts | 2 / 2 |
| Completed tests | 2 / 2 (but only cover hooks + Badge) |
| **Pages with mock-only data** | **3** (Users, Reports, AIInsights fallback) |
| **Hardcoded / dead-end buttons** | **3** (Settings Add Holiday, Reports Export, Reports Email) |
| **Missing global components** | **2** (ErrorBoundary, .env.example) |
| **Missing accessibility features** | **4** (focus trap, skip link, aria-labels, responsive breakpoints) |
| **Missing auth hardening** | **4** (real auth, token expiry, persistent login, forgot password) |
| **Missing build infra** | **5** (vitest config, lint script, vite-env.d.ts, API versioning, dev logging) |

Core application structure is solid. The most critical gaps are **real API wiring for Users/Reports/Holidays**, **auth hardening**, and an **ErrorBoundary** — all of which currently only exist as mock data or are silently omitted.
